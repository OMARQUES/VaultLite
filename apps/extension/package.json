{
  "version": "0.1.0",
  "name": "@vaultlite/extension",
  "type": "module",
  "scripts": {
    "test": "vitest run --environment jsdom",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@noble/hashes": "^1.8.0",
    "@vaultlite/contracts": "*"
  },
  "private": true
}
